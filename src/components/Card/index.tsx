import React from 'react';

import { Container, Title } from './styles';

const Card: React.FC = () => {
  return (
    <Container>
      <Title>Title</Title>
    </Container>
  );
};

export default Card;
